import React from 'react';

export class Profile extends React.Component {

    render(): React.ReactNode {
        return (
            <div>
                PROFILE component
            </div>
        )
    }
}